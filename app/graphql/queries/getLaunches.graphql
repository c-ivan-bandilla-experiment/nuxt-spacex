query GetLaunches($limit: Int, $offset: Int, $find: LaunchFind) {
	launches(limit: $limit, offset: $offset, find: $find) {
		...LaunchFields
		launch_site {
			...LaunchSiteFields
		}
		rocket {
			...RocketsFields
		}
	}
}
